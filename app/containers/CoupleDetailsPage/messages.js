/*
 * CoupleDetailsPage Messages
 *
 * This contains all the text for the CoupleDetailsPage container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.CoupleDetailsPage";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the CoupleDetailsPage container!",
  },
});
