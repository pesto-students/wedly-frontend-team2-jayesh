// import { selectMobileEventsPageDomain } from '../selectors';

describe("selectMobileEventsPageDomain", () => {
  it("Expect to have unit tests specified", () => {
    expect(true).toEqual(false);
  });
});
