/*
 * EinviteEditPage Messages
 *
 * This contains all the text for the EinviteEditPage container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.EinviteEditPage";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the EinviteEditPage container!",
  },
});
