/*
 * EinvitePage Messages
 *
 * This contains all the text for the EinvitePage container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.EinvitePage";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the EinvitePage container!",
  },
});
