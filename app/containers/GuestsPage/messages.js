/*
 * GuestsPage Messages
 *
 * This contains all the text for the GuestsPage container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.GuestsPage";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the GuestsPage container!"
  }
});
