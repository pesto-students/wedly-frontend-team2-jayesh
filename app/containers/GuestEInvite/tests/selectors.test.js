// import { selectGuestEInviteDomain } from '../selectors';

describe("selectGuestEInviteDomain", () => {
  it("Expect to have unit tests specified", () => {
    expect(true).toEqual(false);
  });
});
