// import { selectAccountSettingsPageDomain } from '../selectors';

describe("selectAccountSettingsPageDomain", () => {
  it("Expect to have unit tests specified", () => {
    expect(true).toEqual(false);
  });
});
